结论：`pnextract` 不是默认假设所有孔隙互联。它有真实的连通/邻接逻辑；孔隙之间只有在分割后的孔隙元素相邻接触时才生成 throat。但它对“开放孔隙/封闭孔隙”的处理分两层：

**1. 图像预处理层：可以把孤立孔隙剔除，但不是默认必然执行**

`libvoxel` 里有 `keepLargest0` 命令。文档说明它会保留值为 `0` 的最大连通区域，把其它连通区域设成 `maxT-1`，8-bit 图像里就是 `254`，用于计算有效孔隙度。对应实现是：

- [doc/libvoxel_commands.md](C:/Users/imgw/Documents/Codex/论文复现/pore-scale-simulation-reproduction/pnextract/doc/libvoxel_commands.md:103)
- [src/libvoxel/voxelRegions.h](C:/Users/imgw/Documents/Codex/论文复现/pore-scale-simulation-reproduction/pnextract/src/libvoxel/voxelRegions.h:176)
- [src/libvoxel/voxelRegions.h](C:/Users/imgw/Documents/Codex/论文复现/pore-scale-simulation-reproduction/pnextract/src/libvoxel/voxelRegions.h:189)

这里的逻辑是：先对孔隙体素做 connected-component labeling，然后找到体素数最多的 label，其它孔隙 cluster 被改成 `254`，不再作为 void/pore 进入后续网络提取。

但主程序 `nextract` 只是读图像并执行输入文件里的图像处理命令，不会无条件自动调用 `keepLargest0`。`keepLargest0` 是可用的 `VxlPro`/图像处理命令之一：

- [src/libvoxel/voxelImage.cpp](C:/Users/imgw/Documents/Codex/论文复现/pore-scale-simulation-reproduction/pnextract/src/libvoxel/voxelImage.cpp:400)
- [src/libvoxel/voxelImage.cpp](C:/Users/imgw/Documents/Codex/论文复现/pore-scale-simulation-reproduction/pnextract/src/libvoxel/voxelImage.cpp:514)
- [src/pnm/pnextract/nextract.cpp](C:/Users/imgw/Documents/Codex/论文复现/pore-scale-simulation-reproduction/pnextract/src/pnm/pnextract/nextract.cpp:107)

所以如果输入文件里加了类似 `VxlPro keepLargest0`，封闭/孤立小孔隙会在提取前被剔除；如果没加，它会继续进入网络提取流程。

**2. 球棍网络层：不强行连通，只按真实邻接生成 throat**

网络里有边界伪孔隙节点。默认 `multiDir=false` 时只有 x 方向入口/出口两个边界孔；`multiDir=true` 时有六个方向边界孔：

- [src/pnm/pnextract/inputData.h](C:/Users/imgw/Documents/Codex/论文复现/pore-scale-simulation-reproduction/pnextract/src/pnm/pnextract/inputData.h:123)
- [src/pnm/pnextract/blockNet.cpp](C:/Users/imgw/Documents/Codex/论文复现/pore-scale-simulation-reproduction/pnextract/src/pnm/pnextract/blockNet.cpp:44)
- [src/pnm/pnextract/blockNet.cpp](C:/Users/imgw/Documents/Codex/论文复现/pore-scale-simulation-reproduction/pnextract/src/pnm/pnextract/blockNet.cpp:90)

throat 的生成是在 `VElems` 里扫描 x/y/z 三个方向的相邻元素；只有两个不同的非负 pore id 相邻时，才在两个 pore 的 `contacts` 里插入连接并创建 `throatNE`：

- [src/pnm/pnextract/blockNet.cpp](C:/Users/imgw/Documents/Codex/论文复现/pore-scale-simulation-reproduction/pnextract/src/pnm/pnextract/blockNet.cpp:330)
- [src/pnm/pnextract/blockNet.cpp](C:/Users/imgw/Documents/Codex/论文复现/pore-scale-simulation-reproduction/pnextract/src/pnm/pnextract/blockNet.cpp:353)
- [src/pnm/pnextract/blockNet.cpp](C:/Users/imgw/Documents/Codex/论文复现/pore-scale-simulation-reproduction/pnextract/src/pnm/pnextract/blockNet.cpp:358)

因此它不会把封闭孔隙“假装”接到主孔隙网络里。若某个孔隙 cluster 与边界和其它 cluster 都不连通，它最多会成为一个独立子图，或者在预处理 `keepLargest0` 后被去掉。

**怎么判断开放/封闭**

严格说，这份代码没有看到一个叫 “open pore / closed pore classifier” 的显式分类模块。它的判断方式更像是：

- 开放/贯通孔隙：在输出网络图里，从边界节点 `0/1`，或 `multiDir=true` 时 `0..5`，沿 throats 能访问到的 pore。
- 封闭/孤立孔隙：网络中存在，但从边界节点访问不到的 pore 子图。
- 若用了 `keepLargest0`：非最大连通孔隙已经被改成 `254`，后续一般不会再作为 void pore 进入网络。

所以，对你的复现而言，最稳妥的表述是：`pnextract` 不默认所有孔隙互联；它可以通过 `keepLargest0` 在图像预处理阶段去除孤立孔隙，也可以在网络输出后通过边界节点连通性识别开放孔隙与封闭孔隙。